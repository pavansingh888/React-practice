# React-practice
# Material UI

###learned about how to update state variable of parent component thru child component, by passing updater function as prop in child component which sets/update the state variable of parent component by taking values in argument from child component.